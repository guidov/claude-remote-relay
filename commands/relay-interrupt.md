---
description: Stop the turn currently running on a remote session
argument-hint: [host]
allowed-tools: mcp__remote-relay__remote_interrupt
---

Call `mcp__remote-relay__remote_interrupt`, passing `host` as `$ARGUMENTS` if it is non-empty. Report only whether the interrupt was accepted.
